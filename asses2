#include<stdio.h>
int main()
{
	int a,r,s=0,t;
	scanf("%d",&a);
	t=a;
	while(a>0)
	{
		r=a%10;
		s=s+(r*r*r);
		a=a/10;
	}
	if(t==s)
	printf("%d is ARMSTRONG number",t);
	else
	printf("%d is not an  ARMSTRONG number",t);
	return 0;
}
